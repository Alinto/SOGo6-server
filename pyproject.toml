[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[project]
name = "SOGo"
description = "SOGo 6 backend server"
authors = [
    {name = "Quentin Hivert", email = "quentin.hivert.fr@gmail.com"},
]
license = { text = "GPL-3.0-or-later" }
readme = "README.md"
dynamic = [ "version" ]
requires-python = ">=3.13"
keywords = ["sogo", "webmail", "mail", "calendar"]
dependencies = [
    "click",
    "flask",
    "flask-compress",
    "flask-smorest",
    "marshmallow",
    "pydantic",
    "pydantic-settings",
    "flask-wtf (>=1.2.2,<2.0.0)",
    "python-ldap (>=3.4.4,<4.0.0)",
    "sqlalchemy (>=2.0.42,<3.0.0)",
    "redis (>=6.2.0,<7.0.0)",
    "psycopg (>=3.2.9,<4.0.0)",
]


[tool.poetry]
version = "6.0.0"  # base version
packages = [
    { include = "app" },
    { include = "agent" },
]
exclude = ["tests"]

[tool.poetry.group.dev.dependencies]
pytest = "^7.0.0"
pytest-cov = "^6.1.1"
black = "^25.1"
pylint = "^3.0.0"
pre-commit = "^4.2.0"
mypy = "^1.17.1"


[tool.poetry.requires-plugins]
poetry-plugin-export = ">=1.8"

[tool.poetry.scripts]
# poetry run start
start = "app.run:main"


[tool.pytest.ini_options]
testpaths = [
    "tests"
]

[tool.pylint.basic]
module-naming-style="any"
class-naming-style="PascalCase"
method-naming-style="snake_case"
attr-naming-style="snake_case"
function-naming-style="snake_case"
variable-naming-style="snake_case"
argument-naming-style="snake_case"
disable= [
    "missing-module-docstring",
    "too-few-public-methods",
    "pointless-string-statement",
    "too-many-arguments",
    "too-many-positional-arguments"
]
max-line-length=240
ignored-modules=["psycopg"]

[tool.mypy]
#https://mypy.readthedocs.io/en/stable/config_file.html#
disallow_untyped_defs = true
implicit_optional = true
disable_error_code = ["import-untyped"]
exclude = ["tests"]

[tool.black]
line-length = 120
exclude = 'app/templates'