# The iCalendar format is managed through an external library (icalendar), confined to the
# *Ical.py serializers in app/module/calendar/serializer/. This package mirrors the contact
# module's format/ package, which hand-rolls its vCard and LDIF codecs.
