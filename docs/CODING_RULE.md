# Coding rule

TODO, tell all the rules for uniform coding

Classes's name must be PascalCase
Functions and methods's names must be snake_case
```python
def some_func()
    some_var = 1
    return

class MyClass()
    def my_method()
        return
```

# SOGo parameters
If you need to ass any sogo parameters/settings plus use this format
```
SOGO_{X}_{NAME}
```
* X being S, D or U for respectively system, domain ann user settings
* NAME is the parameter name and must be uppercase snake_case